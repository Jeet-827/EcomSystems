# Single Domain Deployment Notice:
# For single-domain Vercel deployment, leave VITE_API_URL and VITE_ADMIN_API_URL empty!
# The React app automatically resolves relative requests (/api/v1/...) to the live domain.

VITE_API_URL=
VITE_ADMIN_API_URL=
